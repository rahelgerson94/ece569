void printMatrix(float* Mat, int numRows, int cumCols){
	for (int i = 0; i < numRows; i ++){
		for (int j = 0; j < numCols; j ++){
			printf("%.2f", Mat[i*numCols + j];
	}
}

void printMatrix(int* Mat, int numRows, int cumCols){
	for (int i = 0; i < numRows; i ++){
		for (int j = 0; j < numCols; j ++){
			printf("%d", Mat[i*numCols + j];
	}
}
